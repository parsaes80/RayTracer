{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug RayTracer",
            "program": "${workspaceFolder}/RayTracer",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}